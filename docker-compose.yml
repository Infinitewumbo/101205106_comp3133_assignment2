services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      MONGODB_URI: ${MONGODB_URI}
      PORT: 4000
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "4200:80" 
    depends_on:
      - backend
    restart: unless-stopped